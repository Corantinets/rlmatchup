# Configuration locale (optionnel)
PORT=3000

# Configuration Production (Vercel uniquement)
# À configurer dans le dashboard Vercel
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_redis_token

# Note: En local, aucune variable n'est nécessaire.
# L'application utilise un stockage en mémoire (Map) automatiquement.
